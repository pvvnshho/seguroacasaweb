import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        mode: 'light',
        primary: {
            main: '#3751FE',
        },
        secondary: {
            main: '#f50057',
        },
    },
});

export default theme;