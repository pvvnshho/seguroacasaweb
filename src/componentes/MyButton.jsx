import { Button } from "@mui/material"; // Corrige a "Button" con mayúscula

export default function MyButton() { // Cambia el nombre a "MyButton"
    return (
        <Button variant="contained" color="primary"> {/* Usando el componente de Material-UI */}
            Hacer algo
        </Button>
        
    );
}
